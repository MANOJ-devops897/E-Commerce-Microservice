apiVersion: v1
kind: Service
metadata:
  name: product-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: product
  ports:
  - port: {{ .Values.service.port }}
    targetPort: 8081
